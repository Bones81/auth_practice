<h1>Hi,  <%=typeof user === "object" ? user.name : user %></h1>
<% if (typeof user === "object") { %> 
    <a href="/logout">Log Out</a>

<% } else { %>
    <a href="/register">Register</a>
    or 
    <a href="/login">Log In</a>
<% } %>
